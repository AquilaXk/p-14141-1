{
  email {$CADDY_EMAIL}
}

# Cloudflare Tunnel origin: HTTP only between cloudflared and Caddy.
http://{$API_DOMAIN} {
  encode gzip zstd

  # Active backend is switched by deploy/homeserver/blue_green_deploy.sh.
  reverse_proxy back_blue:8080
}
