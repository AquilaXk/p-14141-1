import BlockEditorEngine from "./BlockEditorEngine"
import type { BlockEditorEngineProps } from "./blockEditorEngineTypes"

const BlockEditorShell = (props: BlockEditorEngineProps) => <BlockEditorEngine {...props} />

export default BlockEditorShell
